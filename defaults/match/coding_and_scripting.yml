matches:
  - triggers: ["'binsh", "'#!"]
    replace: "#!/bin/sh"
  - trigger: "'gsm"
    replace: "git switch main && git pull origin main"
  - trigger: "'gpomr"
    replace: "git pull origin main --rebase"
